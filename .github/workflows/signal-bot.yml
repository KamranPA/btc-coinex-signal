name: CoinEx Signal Bot

on:
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Run simple test
        run: |
          echo "🤖 CoinEx Bot Started"
          echo "✅ Workflow is working"
          python -c "print('Python is ready!')"
